server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html index.htm;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Example for proxying API requests if frontend is on port 80 and backend on 8080
  # And you want to access backend via /api route from frontend's domain
  # location /api/ {
  #   proxy_pass http://tire-request-system-backend:8080/api/; # Service name from docker-compose
  #   proxy_http_version 1.1;
  #   proxy_set_header Upgrade $http_upgrade;
  #   proxy_set_header Connection 'upgrade';
  #   proxy_set_header Host $host;
  #   proxy_cache_bypass $http_upgrade;
  # }

  # Example for proxying WebSocket requests
  # location /ws-tire-request/ {
  #   proxy_pass http://tire-request-system-backend:8080/ws-tire-request/;
  #   proxy_http_version 1.1;
  #   proxy_set_header Upgrade $http_upgrade;
  #   proxy_set_header Connection "Upgrade";
  #   proxy_set_header Host $host;
  # }
}
